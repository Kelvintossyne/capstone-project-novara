apiVersion: v1
kind: ConfigMap
metadata:
  name: taskapp-config
  namespace: default
data:
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "taskapp"
  APP_ENV: "production"
  FRONTEND_URL: "http://taskapp.local"
  BACKEND_PORT: "8000"
